# Symfony Sass + Tailwind demo

1. Install dependencies:

```shell
composer install
```

2. Run Sass builder: 
```shell
bin/console sass:build --watch
```

3. Run Tailwind builder:
```shell
bin/console tailwind:build --watch
```

4. Run symfony server:
```shell
symfony serve
```

